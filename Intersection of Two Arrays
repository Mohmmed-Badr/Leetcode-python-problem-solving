class Solution:
    def intersection(self, nums1: List[int], nums2: List[int]) -> List[int]:
         match = []
         for i in nums1:
             for x in nums2:
                 if i == x:
                     if i not in match:
                        match.append(i)
         return match
